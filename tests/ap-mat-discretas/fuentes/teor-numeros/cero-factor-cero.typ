


La mayoría de los libros de esta materia están constantemente, en todas las
definiciones, proposiciones, propiedades, etc., haciendo excepciones sobre
el rango de validez de las variables, es decir, excluyendo de estas algunos
casos particulares. Concretamente, en casos que involucran al $0$. Se debe a
la decisión de excluir al $0$ en el operando de la izquierda en la
definición del operador división exacta. Es decir, encontrará que casi
siempre dicen algo así como "$a eq.not 0$ tal que $a divides b$". En
cualquier caso, esto tampoco evita que en muchas ocasiones aparezcan estas
"verrugas".

Esta decisión es la que se ha tomado tradicionalmente en la teoría de
números, pero últimamente hay quien opta por no seguirla. Es el caso de
@weissman[p. 15]. También, quizás influenciado por el anterior, se sigue
esta regla en @comb-num-th-mileti sección 1.5 Divisivility. En estos, las
definiciones son más amplias y, por ejemplo, en lugar de poner "$a eq.not 0$
en $a divides b$" dicen simplemente "$a divides b$". Si lo piensa, no hay
ningún problema en que sea así. Simplemente, para un $b eq.not 0$ sucederá
siempre que $0 divides.not b$, lo cual no afectaría a la validez de la
proposición. En otras ocasiones sí es necesario excluir que una variable
tome el valor $0$; por ejemplo, en la operación división euclidea.

Estas definiciones, proposiciones, etc. amplias, sin especificar los casos
conflictivos que involucran al $0$, hacen uso de lo que suelo llamar un caso
_vacuamente cierto_ (_vacuously true_), que no es más que, en una
proposición lógica que tiene un condicional (normalmente, representado por
'$arrow.r$' o '$arrow.r.double$'), si el antecedente es falso, el
consecuente será automáticamente cierto, independientemente del valor de
verdad de la otra proposición. Es lo que sucede, por ejemplo, en la
prop-divide-al-multiplo, así como en muchas otras del texto.

Pero, ¿qué sucede para $0 divides 0$? Quizás le "chirríe" ver que admitimos
que $0 divides 0$ es cierto, pues a usted le enseñaron en sus cursos
anteriores de matemáticas que el $0$ no divide a ningún número, o que, como
dicen en las asignaturas de cálculo, 0 dividido entre 0 es una
indeterminación. Sin embargo, si nos ceñimos a la definición del operador
anterior, sí sería cierto. TKTK.

Otra razón que me hace considerar que $0 divides 0$ es que, como se verá en
la sección donde se presenta el máximo común divisor, se ha de admitir que
el máximo común divisor de $0$ y $0$ es $0$. Si se excluye la posibilidad de
$0 divides 0$, esto lo veo como un "parche" o "verruga" a la teoría. Sin
embargo, en caso de admitirlo, es algo que se deduce fácilmente.

En esta asignatura, al principio, parece imposible hacer muchos de los
ejercicios. En realidad, se suelen basar en tener un bagaje de conocimientos
de hechos sobre los números enteros. Por ejemplo, cosas como que un número
par por uno impar da uno par, propiedades sobre el operador división exacta,
y cosas así. Es conveniente, antes de ponerse a hacer ejercicios difíciles,
hacer por uno mismo una gran cantidad de esas demostraciones de propiedades
sencillas. Muchas de estas aparecen en los libros sobre demostraciones,
como, por ejemplo en @proofs-cummings.



